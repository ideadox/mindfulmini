class RoutineLevelModel {
  final String icon, title, subtitle;
  final bool isCompleted;

  RoutineLevelModel({
    required this.icon,
    required this.title,
    required this.subtitle,
    required this.isCompleted,
  });
}
